from: "DGS_O_LSTM_H256_2L_IO.yaml"

backbone:
  args:
    out_channels: 3

target:
  offsets: false
  encoder:
    name: 'bio_tags'
    args: {}
  decoder:
    name: 'bio_tags'
    args: {}

experiment:
  name: "dgs_o_lstm_h256_2l_bio"
